#this command does
sudo apt update
#
sudo apt upgrade
#
sudo apt install parted
#
pip install numpy
#
sudo parted -l && echo -e "\n---\n" && lsb.
#
sudo parted -l && echo -e "\n---\n" && lsblk -f && echo -e "\n---\n"
#
sudo parted -l
#
lsblk -f
#
[ -d /sys/firmware/efi ] && echo "UEFI" || echo "BIOS"